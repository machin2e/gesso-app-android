<resources>

    <!-- Base application theme. -->
    <style name="SplashTheme" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

</resources>