package camp.computer.clay.platform.tasks;

import java.util.UUID;

public class GitTasks {

    public void readFileFromRevision(String filepath, UUID hash) {
        // TODO: Load as binary (or something like that) and add convertToJson, convertToSvg, convertToImage, etc.?
    }

    // Use branches for sideways/equivalent variations on scripts/tasks?
    // Use submodules for task:script relationships?
    // Use submodules for combining tasks into hierarchies?
    // Use submodule:container status to infer notifications to update?
}
